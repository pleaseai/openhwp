version: 2
updates:
  # Keep the SHA-pinned third-party GitHub Actions up to date. Dependabot bumps
  # the pin + comment; `ci` prefix so release-please categorizes the commit.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
